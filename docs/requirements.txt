# Pinned for reproducible, version-proof builds.
# These exact versions are verified to build the site cleanly; CI installs
# only these, so an upstream release can never silently break the site.
mkdocs==1.6.1
mkdocs-material==9.7.6
pymdown-extensions==10.21.2
